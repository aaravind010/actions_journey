name: My first action WF

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: CheckOut
          uses: actions/checkout@v6

          
        - name: Executing Shell Script
          run: ./ascii-script.sh