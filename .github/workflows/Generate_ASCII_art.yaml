name: Generate ASCII art using linuxlogo

on: push

jobs:
    ascii_job:
        runs-on:
            - ubuntu-latest
        steps:
            - name: checkout
              uses: actions/checkout@v6

            - name: Generate ASCII art using linuxlogo
              run: |
                sudo apt install linuxlogo
                linuxlogo >> logo.txt
                cat logo.txt
        